`para rodar o projecto `
 npx expo start --tunnel -c  
`instalar dependecias`
npm install
 `instalar expo`
 npm install expo    

 `dependencias`
 npm i jwt-decode 

 

```sh
# Using npm
npm start

# OR using Yarn
yarn start
```
